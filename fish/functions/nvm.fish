function nvm
  bass source ~/.nvm/nvm.sh --no-use ';' nvm $argv
end

# need to install fisher,
# ` curl -sL https://git.io/fisher | source && fisher install jorgebucaran/fisher `
# then ` fisher add edc/bass `
